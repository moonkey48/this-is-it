{
  "name": "context-capture",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "icons": "node scripts/generate-icons.mjs",
    "validate:chrome": "node scripts/validate-extension.mjs",
    "package:chrome": "node scripts/package-extension.mjs",
    "test": "npm run validate:chrome"
  },
  "engines": {
    "node": ">=20"
  }
}
